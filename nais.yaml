image: "repo.adeo.no:5443/integrasjon/pdfgen"
replicas:
  min: 1
  max: 4
port: 8080
healthcheck:
  liveness:
    path: /is_alive
    initialDelay: 5
    periodSeconds: 5
    failureThreshold: 30
  readiness:
    path: /is_ready
    initialDelay: 5
    periodSeconds: 5
    failureThreshold: 30
prometheus:
  enabled: true
  path: /prometheus
ingress:
  enabled: true
resources:
  limits:
    cpu: 2000m
    memory: 1Gi
  requests:
    cpu: 50
    memory: 128Mi
redis: true
fasitResources:
  exposed:
  - alias: pdfgen
    resourceType: RestService
    path: /api
